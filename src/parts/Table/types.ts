export type SortConfigType = {
  columnId: string | null
  direction: 'asc' | 'desc'
}

export type ThemeColors = {
  textColor: string
  accentColor: string
  headerBg: string
  rowBg: string
  borderColor: string
}