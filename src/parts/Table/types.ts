export type SortConfigType = {
  columnIndex: number | null
  direction: 'asc' | 'desc'
}

export type ThemeColors = {
  textColor: string
  accentColor: string
  headerBg: string
  rowBg: string
  borderColor: string
}